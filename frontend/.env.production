# Loaded automatically by Create React App during `npm run build`.
# In production the SAME Express server serves both the built React app and the
# API, so the frontend talks to its own origin: a relative "/api" for REST and
# an empty socket URL (socket.io then connects to window.location automatically).
REACT_APP_API_URL=/api
REACT_APP_SOCKET_URL=
