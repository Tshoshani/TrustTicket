# Loaded automatically by Create React App during `npm start` (local dev).
# In dev the backend runs on a different port (3000) than the React dev server,
# so the frontend must call it with an absolute URL.
PORT=5173
REACT_APP_API_URL=http://localhost:3000/api
REACT_APP_SOCKET_URL=http://localhost:3000
